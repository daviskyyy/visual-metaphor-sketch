---
name: visual-metaphor-sketch
description: 把一句观点、文章段落或抽象关系转成极简、无字的“一图胜千言”视觉隐喻，优先用上下或左右并排对比展现同一条件下的不同结果。适用于概念插画、公众号正文配图、少字手绘解释图、风险与缓冲、家庭关系、选择与结果等主题；只生成独立插图，不带截图正文、卡片界面或水印。
---

# 一图胜千言简笔画

将抽象判断变成一个看得见的物理关系。先想清楚“画面里发生了什么”，再决定颜色和笔触。生成的是**独立插图画布**，不是整张截图或带文案的社交媒体卡片。

## 工作流程

1. 从输入中抽出一个可以被画出来的判断：对象、关系、转折、读者应一眼看懂的结论。若有多个判断，拆成多张图；不要在一张图里堆满。
2. 提出 2–3 个不同的视觉隐喻，标出各自的“一眼识别点”和可能误读之处。涉及有无、前后、选择或缓冲时，优先考虑上下或左右并排的两种状态；其他关系可用单场景。对于同一系列图片，重复人物造型和线条气质，但更换核心隐喻。
3. 选最少元素也能讲清的方案。对比画面先锁定相同的主体、时间起点与外部冲击，只改变一个关键条件；把差异画在动作、位置、受力及后果上。风险场景要在两边都发生同等冲击，缓冲只减轻后果，不让风险消失。需要表示时间时，优先用同一人物或物体的重复与变化，避免另添装饰性季节图标。
4. 先写一句**无文字画面测试**：“如果遮住所有文字，读者会看到……，因此理解……”。测试不过就重构，不靠标题救画面。
5. 按“纯插图边界”组装生成提示词。用当前环境可用的图像生成工具直接出图；如果用户只要构思或提示词，就不要擅自生成。要精确曲线、坐标、数值或版面文字时，改用 SVG/HTML 绘制或后期排字，不让图片模型猜。
6. 查看成图，缩小到公众号手机正文宽度检查：无字时能否区分两种状态？结果是否体现在人身上，而不只是道具轻微变化？如果末尾两个结果重新变得一样，继续修改。检查裁切、文字和事实边界；失败时针对具体误读重新生成。交付时可在聊天文字里解释图意，不把解释句画进图里。

## 从参考截图提炼的视觉语法

需要更细的观察、隐喻菜单和质检例子时，读 [references/visual-grammar.md](references/visual-grammar.md)。参考图是**风格分析材料，不是要临摹的模板**；不要复刻某张的构图、人物动作、标语或品牌。

- 白或暖白画布，四周大量留白；不画背景风景或外层应用卡片。
- 近黑或深暖灰的少量均匀手绘线条；线条、轮廓和动作准确，画得像观念草图，不像精致绘本。
- 默认仅用黑/灰线与一种强调色；必要时再加一种表示共同外力的低饱和色。无渐变、阴影、水彩纹理和装饰。
- 圆头线条小人，不画五官、服装和手指细节；通过站姿、倾斜、抓扶、距离和高度讲故事。
- 尽量并排重复同一元素，使改变条件后的波动幅度和后果一眼可比。只保留人物、关键物体、外力与缓冲装置；删去云朵、花草、密集水纹等不承担信息的东西。
- 图内严格**无文字**，包括标题、标签、数字和图例；无字读不懂就修改构图。只有用户明确要求图内文字时才加入。

## 纯插图边界

参考是手机截图，只分析其中矩形插图区域。新画面不要包含：手机状态栏、返回键、话题标签、表情互动、卡片圆角/边框、滚动条、评论、插图下方的粗体标题或长正文、署名、水印、Logo。不要为了“像截图”而放入这些元素。

默认输出一张无边框横版插图，建议 3:2 或 16:9，主体周围保留安全留白，便于公众号正文使用。用户指定竖版、方图或尺寸时从其要求。不要把下方文案嵌进画布；需要配文时单独写在回复中。

## 提示词组装

用以下结构而非照抄固定场景：

> 只生成一张独立的极简视觉隐喻简笔画。核心观点：[一句话]。优先构图：[上下或左右并排]；两边相同的主体、起点、风险：[内容]；唯一不同的条件：[内容]；可见后果：[两边的人物动作、位置、物体状态和差异幅度]。若为过程，沿同一方向重复人物或物体表示时间。风格：暖白底、大留白、少量均匀近黑手绘线、空心圆头线条小人、不画五官服装、最多一两处低饱和色块；只保留主角、关键物体、外力和起作用的装置。严格无任何文字、数字、图例、景观装饰、截图 UI、卡片、logo、水印、渐变和复杂纹理。不要复制任何参考画面的具体构图。

## 语义与安全

- 用物理隐喻解释风险时，不把缓冲误画成阻止事件发生，不暗示保险保证赚钱、治愈或绝不返贫。
- “先保障家庭支柱”不等于只保护爸爸；同时考虑收入和照护劳动。根据用户真实家庭结构表达。
- 如果画面涉及医疗、金融、法律等事实，先核验具体主张，或改成非定量、非承诺性的概念图；图像不替代产品条款或专业建议。
- 一系列作品保持视觉身份一致，但每张图只传达一个观点。
